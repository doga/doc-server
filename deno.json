{
  "tasks": {
    "generate-tls-certificate": "openssl req -x509 -newkey rsa:2048 -keyout $TLS_CERT_KEY -out $TLS_CERT -days 365 -nodes",
    "server": "deno run --allow-env --allow-net --allow-read ./main.ts"
  }
}
